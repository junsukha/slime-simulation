#ifndef CONST_HPP
#define CONST_HPP

const int WINDOW_WIDTH  = 1280;
const int WINDOW_HEIGHT = 720;

const int NUM_PARTICLES = 100000;
const float PARTICLE_RADIUS = 0.5f;

const float DECAY_FACTOR = 0.95f;
const float DEPOSIT_AMOUNT = 100.0f; 

const float SENSOR_ANGLE      = M_PI / 6;
const float SENSOR_DISTANCE   = 20.0f;
const float ROTATION_ANGLE    = M_PI / 6;

#endif // CONST_HPP